apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: default-pool
  namespace: metallb-system
spec:
  # TODO: Update this IP range based on your network configuration
  # This should be a range of IPs on your local network that are not used by DHCP
  addresses:
    - 192.168.1.200-192.168.1.250
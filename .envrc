#!/usr/bin/env bash

# Set Kubernetes context to rke2-proxmox
export KUBECONFIG="${KUBECONFIG:-$HOME/.kube/config}"
kubectl config use-context rke2-proxmox

echo "Switched to Kubernetes context: rke2-proxmox"